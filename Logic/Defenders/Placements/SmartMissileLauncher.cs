using UnityEngine;
using System.Collections;

/*
 * The idea behind the smart missile launcher is that it targets higher priority ships.
 * Is this desired? Or instead should it be a missile with other properties.
 */ 
public class SmartMissileLauncher : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
